<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ireos.testemail">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_test_mail"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_test_mail_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sent.MailActivity"
            android:theme="@style/AppTheme.OverlapSystemBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sentdetail.MailDetailActivity" />
        <activity android:name=".senteditmail.SentEditActivity" />
    </application>

</manifest>